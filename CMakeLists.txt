if("${ARCH}" STREQUAL "x86")
  add_subdirectory(simd_ops)
endif()
#if("${ARCH}" STREQUAL "AArch64")
#  add_subdirectory(simd_ops_arm64)
#endif()
#if("${ARCH}" STREQUAL "ARM")
#  add_subdirectory(simd_ops_arm)
#endif()
